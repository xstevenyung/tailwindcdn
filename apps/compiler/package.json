{
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && npx netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/functions": "^1.0.0",
    "postcss": "^8.4.14",
    "tailwindcss": "^3.1.4"
  },
  "devDependencies": {
    "netlify-cli": "^10.7.1",
    "typescript": "^4.7.4"
  }
}
